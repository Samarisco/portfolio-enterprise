{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },

    "@portfolio/types#build": {
      "outputs": []
    },

    "@portfolio/utils#build": {
      "outputs": []
    }
  }
}